package org.twins.core.enums.sort;

/**
 * Sortable fields for {@code twin_pointer} search.
 */
public enum TwinPointerSortField {
    name,
    pointerFeaturerId,
    twinClassId,
    optional,
    id,
    createdAt,
    createdByUserId
}
